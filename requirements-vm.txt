# Requirements for VM-optimized Traffic Collector Daemon
# Minimal dependencies for production deployment

# Core dependencies
requests>=2.28.0
pyyaml>=6.0
schedule>=1.2.0
pandas>=1.5.0
numpy>=1.24.0

# Database
sqlite3  # Built into Python, but listed for clarity

# Daemon and process management
python-daemon>=3.0.0
psutil>=5.9.0

# API server for health checks and status
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Security
PyJWT>=2.8.0

# Logging and monitoring
structlog>=23.0.0

# Development and testing (optional)
pytest>=7.0.0
pytest-cov>=4.0.0